export {};
/**
 * 
 const arr = [
     {
         key: 'some',
         label: 'something',
         value: 'nothing',
     },
 ];
 // arr.reduce((acc,curr)=> (acc[curr]='',acc),{});
 console.log(
     arr.reduce((obj, el) => {
         return (obj[el.key] = el.value), obj;
     }, {})
 );
 * 
 * 
 */
